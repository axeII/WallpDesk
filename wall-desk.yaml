pixiv: 
        command: ["mv","-n","{HOME}/Desktop/{n[0]}"]
        second: [f"{HOME}/Pictures/pix-girls/{n[1]}"]
girls: 
        command: subprocess.call(["mv","-n",f"{HOME}/Desktop/{n[0]}",
        f"{HOME}/Pictures/Madchen/{n[1]}"]),
img:
        command: subprocess.call(["mv","-n",f"{HOME}/Desktop/{n[0]}",
        f"{HOME}/Pictures/{n[1]}"]),
meme: 
        lambda n : subprocess.call(["mv",
        "-n",f"{HOME}/Desktop/{n[0]}",
        f"{HOME}/Pictures/Meme/{n[1]}"]),
daytime:
        command: lambda n :
        subprocess.call(["mv","-n",f"{HOME}/Desktop/{n[0]}",f"{HOME}/TimeDayWal/{n[1]}"]),
mv:
        command: lambda f, t :
        subprocess.call(["mv","-n",f"{HOME}/Desktop/{f}",f"{os.path.abspath(t)}"]),
trash:
        command: lambda n :
        subprocess.call(["mv",f"{HOME}/Desktop/{n[0]}",f"{HOME}/.Trash/{n[1]}"]),
wall:
        command: desktop(n[0], n[1])
